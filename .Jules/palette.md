## 2024-05-15 - Missing ARIA Labels on Close Buttons
**Learning:** Found a pattern of icon-only close buttons (using lucide-react `X` icon) in modals and notifications lacking `aria-label` attributes across the application.
**Action:** Always verify icon-only buttons have descriptive `aria-label`s when implementing or reviewing new UI components.
